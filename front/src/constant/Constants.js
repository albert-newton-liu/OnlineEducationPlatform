// Base URL for API requests, configurable via environment variables
export const API_BASE_URL = import.meta.env.VITE_BACKEND_DOMAIN;

// Content types for course templates
export const TextType = 0;

// Content types for course templates
export const ImageType = 1;

